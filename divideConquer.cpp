/**
 * @author max niu , matthew wilbern
 * @date Nov 22, 2019
 * @copyright 2019
 */

 #include <iostream>
 #include <fstream>
 #include <vector>
 #include <math.h>

    struct point {
    int x;
    int y;
 };

    double dist(point p1, point p2) {
    return sqrt(pow(p1.x - p2.x, 2) +  pow(p1.y - p2.y, 2) * 1.0);
    }

    int main() {
 }

    void DivideAarray(vector<point> p) {
    int mid = p

    .size()/2;


    for(int i = 0;i < mid; i++)


    }
